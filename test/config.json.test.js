/*
 * test config.json
 */
var assert = require("assert");
describe("test config by file 'config.json'", function() {

    it("test", function(done) {
        var n = 1;
        assert.ok(n);
        done && done.call(this);
    });
});
