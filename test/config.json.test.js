/*
 * test config.json
 * coming soon...
 */
var assert = require("assert");
describe("test config by file 'config.json'", function() {

    it("test0", function(done) {
        var n = 1;
        assert.ok(n);
        done && done.call(this);
    });
});
